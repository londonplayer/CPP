#include <iostream>
using namespace std;

int main()
{
    char grade = 'A';         // "char" stores a single character;
    string phrase = "phrase"; // "string" stores any type of plain text;
    int age = 50;             // "int" is used to store whole numbers only;
    float decimal = 10.5;     // "float" stores any decimal number with a limited amount of characters;
    double decimalL = 1.3213; // "double" stores decimal number with a larger amount of characters;
    bool isMale = true;       // "bool" stores two types of data: true and false;

    cout << grade << endl;

    return 0;
}